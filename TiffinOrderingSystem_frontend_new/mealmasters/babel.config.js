module.exports = {
  presets: ['babel-preset-expo'],
  plugins: [
    // 'react-native-worklets/plugin', // Add this line
    'react-native-reanimated/plugin', // Usually required as well
  ],
};

//Need to do

// npm install react-native-worklets@0.5.1